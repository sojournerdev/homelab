---
galaxy_info:
  role_name: gpu
  author: homelab
  description: >
    Installs NVIDIA container toolkit so K3s auto-detects GPU runtimes.
    Verifies runtime binary and GPU accessibility after install.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - nvidia
    - gpu
    - containerd
    - kubernetes
dependencies: []
