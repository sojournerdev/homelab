---
galaxy_info:
  role_name: workstation
  author: sojournerdev
  description: >
    Configures the local Ansible controller: installs packages, distributes
    SSH access to the server, fetches kubeconfig, and validates SOPS
    recipient against the local age key.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: MacOSX
      versions:
        - all
  galaxy_tags:
    - workstation
    - controller
    - bootstrap
dependencies: []
